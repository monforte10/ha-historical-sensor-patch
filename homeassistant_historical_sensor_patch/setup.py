from setuptools import setup, find_packages

setup(
    name="homeassistant-historical-sensor-patch",
    version="2.0.0.post1",
    packages=["homeassistant_historical_sensor_patch"],  # detectará homeassistant_historical_sensor_patch
    install_requires=[],
)
